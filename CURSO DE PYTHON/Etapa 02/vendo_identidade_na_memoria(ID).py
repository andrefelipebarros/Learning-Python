import os

os.system("CLS || CLEAR")

#Vendo o ID da variável na memória:

v1 = "memoria_id"
v2 = "memoria_id"

print(id(v1))
#Pytho identifica que tem a mesma string \
# então coloca o mesmo id para os dois
print(id(v2))